apiVersion: v1
kind: Service
metadata:
  name: discord-mcp-server
  namespace: discord-agent-mcp
  labels:
    app: discord-mcp-server
spec:
  type: ClusterIP
  selector:
    app: discord-mcp-server
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
